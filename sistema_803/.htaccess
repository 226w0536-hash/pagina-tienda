<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Enviamos toda la ruta (incluyendo parámetros) a un parámetro 'url'
    RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]
</IfModule>
